Dependencies: jupyter, pandas, numpy, beautifulsoup4, json, sqlite3

1. Install python3 environment
2. pip install -upgrade pip
3. pip install xxx 
   where xxx is in [jupyter, pandas, numpy, beautifulsoup4, json, sqlite3]